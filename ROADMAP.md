# 项目升级路线图

## 当前已完成事项

1. 项目结构分析
   - 已生成 `FRAMEWORK.md`。
   - 已梳理目录结构、测试执行流程、Base 模块、Page Object、测试文件分类、依赖和已知问题。

2. Codex 工作约束
   - 已生成 `CODEX_GUIDE.md`。
   - 明确后续每次工作前需阅读 `FRAMEWORK.md`，不改目录结构、不删测试、不改业务流程。

3. 项目 README
   - 已生成 `README.md`。
   - 说明项目介绍、环境要求、运行方式、BeautifulReport、截图位置、报告位置、新增测试流程和常见问题。

4. 路径工程化第一阶段
   - 已新增 `base/project_path.py`。
   - 已将报告、截图、验证码图片等路径迁移到项目相对路径。
   - ChromeDriver 路径仍保留历史写法，未在该阶段处理。

5. 依赖清单整理
   - 已生成 `requirements.txt`。
   - 根据当前 import 整理第三方依赖，未固定版本。
   - `minium` 已标注为小程序测试可选依赖。

6. 长期维护规则
   - 已生成 `PROJECT_RULES.md`。
   - 已定义项目修改原则、Codex 工作规则、禁止事项、验证要求、Git 提交规范、UI/API/配置维护规则。

## 后续升级阶段

### 阶段一：补齐基础工程文件

目标：

- 完善 `.gitignore`，避免提交缓存、报告、截图和本机临时文件。
- 校正 README 中与 `requirements.txt` 不一致的依赖安装说明。
- 明确 Python 推荐版本，但不强行锁定。

风险：

- 低。
- 主要影响文档和仓库维护，不改变测试运行逻辑。

建议：

- 优先执行。
- 每次只处理一个文件或一个主题。

### 阶段二：运行入口说明和分组

目标：

- 明确 UI、API、小程序测试的运行入口。
- 保持 `test_case/run.py` 现有默认行为。
- 文档化哪些命令会启动浏览器、访问接口或连接数据库。

风险：

- 低到中。
- 如果修改 `run.py`，可能影响 BeautifulReport 现有生成流程。

建议：

- 先只做文档。
- 如需修改入口脚本，必须单独评估。

### 阶段三：ChromeDriver 配置工程化

目标：

- 处理 `D:\Python310\chromedriver.exe` 这类历史硬编码路径。
- 支持本机环境差异。
- 保持现有 Windows 路径兼容。

风险：

- 中。
- 可能影响 `test_shanghu.py`、`test_xinlian.py`、`test_lhcz.py` 的浏览器启动。

建议：

- 先设计配置读取方式。
- 修改前询问确认。
- 不同时改登录流程和断言。

### 阶段四：敏感配置外置化

目标：

- 将账号、密码、Token、数据库连接、接口地址等迁移到环境变量或本机配置。
- 避免在新增文档和代码中继续扩散敏感值。
- 保持旧配置兼容，避免一次性破坏运行。

风险：

- 中到高。
- 涉及 UI 登录、API 请求、数据库验证码读取。
- 配置缺失会导致测试无法运行。

建议：

- 分模块执行：先数据库，再 API，再 UI 账号。
- 每一步都保留清晰错误提示。
- 不在同一阶段修改业务断言。

### 阶段五：测试稳定性优化

目标：

- 用显式等待逐步替代关键路径上的 `time.sleep()`。
- 优先优化登录、验证码、页面跳转、消息提示等不稳定点。
- 保留测试步骤和断言语义。

风险：

- 中。
- 等待条件写错会引入新的不稳定。

建议：

- 一个用例或一个页面流程一批。
- 修改后需要在可用环境中运行真实 UI 测试验证。

### 阶段六：Page Object 扩展

目标：

- 将 `test_lhcz.py`、`test_yichuang.py` 中直接写在测试里的页面操作逐步沉淀到 `page_object/`。
- 降低测试脚本重复操作。
- 提高页面流程复用性。

风险：

- 中到高。
- 属于重构，可能影响多个 UI 用例。

建议：

- 必须先询问确认。
- 每次只迁移一个页面或一个业务流程。
- 不改变测试断言。

### 阶段七：API 测试规范化

目标：

- 区分 `test_api.py` 这种 unittest 测试和 `api.py` 这种调试脚本。
- 避免导入即请求的脚本被误用。
- 明确接口测试数据和断言策略。

风险：

- 中。
- API 地址、Token、测试数据可能随环境变化。

建议：

- 先文档化，再调整脚本。
- 不直接替换现有请求参数。

### 阶段八：BeautifulReport 集成修复或替换评估

目标：

- 修复当前内置 `BeautifulReport` 中 stdout/stderr、图片路径、异常处理等可疑实现。
- 或评估是否继续维护内置版本。

风险：

- 高。
- 报告库是当前主执行链路的一部分，影响所有通过 `run.py` 生成报告的测试。

建议：

- 不作为早期任务。
- 先用最小样例验证报告行为。
- 修改前必须询问确认。

### 阶段九：清理历史产物

目标：

- 清理或忽略 `.DS_Store`、`__pycache__`、历史截图、历史 HTML 报告、运行日志等。
- 减少仓库噪音。

风险：

- 中。
- 删除历史报告或截图可能影响回溯。

建议：

- 先加 `.gitignore`。
- 是否删除历史产物必须由用户确认。

## 推荐执行顺序

1. 完善 `.gitignore` 和基础维护文档。
2. 校正 README 与 `requirements.txt` 的依赖安装说明。
3. 明确测试运行入口和真实环境风险。
4. 工程化 ChromeDriver 配置。
5. 分阶段外置敏感配置。
6. 优化关键 UI 等待。
7. 扩展 Page Object。
8. 规范 API 测试和调试脚本边界。
9. 评估 BeautifulReport 修复。
10. 清理历史产物。

总体原则：先做不改变运行逻辑的维护性改动，再处理环境配置，最后处理会影响测试行为的重构和稳定性优化。

# Open Web runtime dependencies.
# Versions are intentionally not pinned because the project does not declare
# tested package versions.

# Selenium UI automation
selenium

# YAML configuration and test data
PyYAML

# BeautifulReport PDF export
playwright

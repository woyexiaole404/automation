# Codex 工作指南

以后每次开始处理本项目任务时，必须先遵守以下规则。

## 开始前

1. 必须先阅读 `FRAMEWORK.md`，基于当前项目真实结构和流程工作。
2. 默认保持当前项目风格，不引入无关的新框架、新目录或新抽象。
3. 每次只解决一个明确问题，不顺手处理无关问题。

## 禁止事项

1. 不允许修改目录结构。
2. 不允许删除任何测试用例。
3. 不允许修改业务流程。
4. 不允许未经确认直接做重构。

## 修改范围控制

1. 如果任务涉及重构，必须先询问并获得确认，不能直接修改。
2. 如果预计或实际修改超过 10 个文件，必须停止并说明原因。
3. 修改时优先选择最小改动，保持现有代码调用方式和执行方式兼容。

## 完成后验证

修改完成后必须执行：

```bash
python -m compileall .
```

如果当前环境没有 `python` 命令，可以补充执行：

```bash
python3 -m compileall .
```

但必须明确说明 `python -m compileall .` 是否成功。

## 最终输出

每次完成后必须输出：

1. 修改了哪些文件。
2. 为什么修改。
3. 如何验证。

如果有未完成或无法验证的事项，必须明确说明原因。
